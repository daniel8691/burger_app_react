import React from 'react';
import './Toolbar.css'
// import logo files
import Logo from '../../Logo/Logo'

const Toolbar = (props) => (
    <header className="Toolbar">
        <div>MENU</div>
        <Logo />
        
        <nav>
            ...
        </nav>
    </header>
);

export default Toolbar


